# 更新日志

本项目的版本号采用日期化规范（`vYYYYMMDD`），每次发布对应一个 tag。

## 未发布

## v20260815-beta.2（2026-08-14）

### 修复

- 延迟展示不可自动恢复的微信登录失效状态，并抑制自动重连期间的重复网络错误提示。
- 根据青酿每轮总金币与操作回包校正单价展示，避免协议价格数组错配。
- 保留活动操作成功但快照刷新失败时的未知状态，并禁用重复提交入口。

## v20260815-beta.1（2026-08-13）

### 新功能

- 新增 macOS 游戏资源同步与离线完整性检查命令，生成可随 Bot 发布的静态资源包。
- 新增管理员 GitHub Tag 版本检查与右上角更新提醒。
- 新增「青酿换万金」活动查询与操作入口。

### 重构

- 使用 `ResourceBundle` 统一物品、植物及图片展示资源查询。
- 将游戏图片改为内容寻址资源，并提供不可变缓存响应。
- 合并活动快照请求并复用背包结果，治理页面重复加载和启动阶段的请求拥塞。
- 删除 `Update.log`，将发布记录统一维护在本文件。

### 修复

- 原子预占游戏请求槽并为心跳保留容量，避免并发编码绕过请求上限。
- 去除登录后的重复背包读取，错开非关键自动任务并对齐活动请求超时边界。
- 对齐新版物品使用与分享协议，收紧物品出售权限来源。

### 已弃用

- 弃用 `scripts/sync-seed-assets.mjs`，兼容入口将在后续稳定版本中删除。

### 已知限制

- 新网关协议的逐请求 token 尚未补齐，真实登录后的业务回包及青酿流程仍待验证；本版本仅用于测试。

## v20260814（2026-08-12）

### 重构

- **移除 yyb-go 依赖**：Docker Compose、README、管理面板与微信登录配置不再包含 yyb-go 或第三方代理模式；微信扫码登录统一使用进程内置协议
- **微信凭证操作统一收口主进程**：worker thread/fork 通过 IPC 请求 code 刷新和凭证保活，所有入口按账号串行执行，避免滚动 refresh token 被并发请求相互废弃

### 修复

- **重扫凭证优先级**：扫码换 code 显式绑定扫码 UUID、当前用户和编辑账号，账号保存成功后再消费会话，避免重扫时误用已失效的持久化凭证
- **滚动 token 丢失**：refresh token 刷新成功但 loginBuffer 获取失败时仍持久化新 token，防止旧 token 已失效、新 token 未落库导致 `-109 / 40030`
- **刷新响应兼容**：兼容 snake_case/camelCase 两种 token 与 loginBuffer 响应结构，并对错误日志中的 token 脱敏
- **跨用户扫码隔离**：微信扫码 API 纳入登录鉴权，扫码会话绑定所属用户，禁止跨用户查询、换 code 或写入账号凭证
- **账号级刷新互斥**：启动刷新、掉线重登、定时保活和编辑重扫共用稳定的 accountId 锁，换绑 wxid 时也不会被旧请求覆盖
- **worker 退出清理**：退出时清理 scheduler、统计定时器和待处理 IPC 请求，停止后的异步刷新不会重新注册旧 code 重连任务
- **getFarmCode 请求合并**：同一账号并发换 code 请求复用在途结果，降低微信协议请求和风控压力

## v20260813（2026-08-12）

### 新功能

- **观星自动点亮**：赛季活动「千星同明」每日星宿奖励自动点亮（6h 轮询，活动缺失静默），梅酒种子（29003）持续入包，返场活动可攒 2x2 梅酒
- **战令/每日任务推送驱动**：BattlePassChangeNotify / TaskInfoNotify 推送到达秒领（30s 轮询兜底）
- **worker 卡死自愈（watchdog）**：主线程 30s ping 探活，90s 无响应判定卡死强制重启（连续 3 次退避停止），挂机不再假运行
- **2x2 种植修正**：24 块 1x1 布局下种 4 格种子自动找相邻 4 格 + `auto_slave` 服务端自动整合（旧主格筛选逻辑在全 1x1 布局永远找不到主格，2x2 种子被跳过）；面板显示从属/合种标记
- **活动物品名映射**：梅酒种子/果实、星砂、星砂商店装饰等 19 个活动物品显示真实名称

### 修复

- **refresh_token_auth 字段 camelCase（-109 根因）**：snake_case 时服务器解析不到 refreshtoken 报「RefreshToken empty token」，改 camelCase 后保活实测成功
- **扫码登录误报**：轮询超时 25s→60s（MMTLS 冷启动握手 30-48s），超时不再误弹「重新扫码」（恢复等待继续轮询）；AccountModal 加 pollingLock 防请求堆积
- **批量卖出超限**：面板全选卖出 >19 个物品报错（服务器单请求 items 上限），按 15 个/批自动分批；失败时刷新背包避免重复卖

## v20260812（2026-08-12）

### 新功能

- **微信登录凭证保活（loginBuffer 不再 1 小时失效）**：
  - loginBuffer 失效自动续期（refreshtoken 滚动刷新换新凭证，无需重新扫码）
  - 挂机账号每 30 分钟定时刷新凭证（长期挂机不掉线也持续续期，随时掉线秒连）
  - 账号持久化 refreshtoken（滚动续期总是更新，旧值不残留）
- **商店种植路径支持 2x2 主格**：非背包策略（商店购买）下 2x2 作物自动筛主格种植，无主格跳过不购买；失败学习与背包路径共享（2x2 配置缺失不再反复购买浪费金币）

### 修复

- refreshLoginBuffer 调用缺少 cookie jar 导致 TypeError（自动续期失效）——review 修复
- 账号更新接口允许 body 覆盖微信凭证（凭证覆盖防护）——review 修复
- 微信账号换绑 wxid 时旧凭证残留——review 修复
- 商店路径 2x2 金币不足时种子数未 clamp 主格数

## v20260811（2026-08-11）

### 新功能

- **微信登录原生化（进程内应用宝协议，不再依赖外部 yyb-go 服务）**：
  - `wx-login` 模块：微信 MMTLS 加密握手 + 开放平台扫码全流程（二维码生成/轮询/授权/换 code）
  - loginBuffer 登录凭证持久化到账号，自动重登/手动启动自动刷新 code（无需重新扫码）
  - 微信真实昵称 + 头像显示（应用宝用户信息接口；Dashboard 账号卡片新增头像）

### 修复

- MMTLS 握手加速：socket 超时 30s→5s、端口优先级排序（8080 优先）、尝试范围扩大——扫码/刷新 code 不再长时间 pending
- 登录 code 刷新失败限次 3 次停止（原无限 5s 重试循环），重试间隔拉长到 30s 降低风控触发
- 账号启动不再阻塞等待 code 刷新（fire-and-forget，启动响应 6s→13ms）
- 扫码轮询防请求堆积（长轮询串行化）+ 轮询/全局超时兜底（axios 10s→30s）
- OAuth code 重放防护（confirm 幂等）、账号创建防 loginBuffer 注入
- 微信头像 URL 白名单（仅 qlogo.cn 域名）防 SSRF

## v20260808（2026-08-08）

### 新功能

- **新野兽派 neo 主题**（参照 Vaultr DesignNeo.md 设计规范）：
  - 主题面板新增「野兽派」选项（白底 #fff / 黑字 #111 / 品牌黄 #eab308）
  - 全局 0 圆角 + 纯黑 2px 边框 + 硬阴影（2-4px 纯黑偏移，零模糊）
  - 品牌黄侧边栏 + active 深黄选中态（黑指示条）
  - 主操作按钮统一品牌黄 + 黑边框 + hover 按下位移
  - 登录页网格背景 + 白卡硬阴影；main 结构分割线 2px #333
  - **活动中心豁免**：星空独立设计不跟随主题（宽泛规则显式排除）

## v20260807（2026-08-07）

### 重构

- **移除重复的独立自动捣蛋实现**，复用好友巡查内置的「自动捣乱」（设置 → 好友互动 → `自动捣乱` 开关）：
  - 原独立实现（trick.js）与 friend.js 巡查逻辑重复（后者更完整：预检查可捣乱地块、远程剩余次数、Enter 上下文、限流处理）
  - 删除 trick.js 及 worker/store/设置面板的对应配置与开关
- **修复放虫/放草请求对齐线上格式**：`PutInsects/PutWeeds` 请求补 `field_4=2`（操作类型字段，抓包确认），确保巡查自动捣乱真正生效

## v20260806（2026-08-06）

### 新功能

- **神秘商人** — 限时 NPC 商品查询与购买：
  - 协议：`mysteryshoppb.MysteryShopService`（GetActiveNPC 查询 + Buy 购买，抓包反推验证）
  - 后端：`mystery-shop.js`（查询/购买/余额读取/商品名图）+ `/api/mystery-shop` 与 `/api/mystery-shop/buy`
  - 前端：侧边栏 🎩 独立页面（限时倒计时、商品卡片、数量选择、余额不足禁用）
- **抓包字段级验证工具** — `core/tools/decode-captures.js`：
  - 零硬编码类型映射（自动按 service/method 匹配请求/响应类型）
  - 请求明文/加密双通道解码 + roundtrip 重编码校验（字段级差异定位）
  - `--md` 输出字段级协议文档
- **协议字段补全** — 20 组协议 roundtrip 一致（请求帧 100%）：Login/GetSeasonInfo/Mall/Heartbeat/Harvest/Enter/Leave/PutInsects/PutWeeds 请求字段 + LandUpgradeCondition 价格/Buff/PlantInfo 等响应结构

### 其他

- README 更新（神秘商人/好友互动说明）

## 旧版历史（2026 年 3 月）

以下内容由原 `Update.log` 压缩迁移，仅保留已实现的技术变化。

### 2026-03-31

- 移除好友列表一键操作，修复单个好友操作与好友倒计时
- 将好友互动拆分为偷菜和帮助策略，优化肥料购买检测

### 2026-03-27 至 2026-03-29

- 重构自动购买化肥，增加土地进度展示与界面图标
- 修复注册、卡密复制和登录页版本同步，增加功能说明
- 重构登录与 Token 安全机制，增加登录审计、速率限制、锁定和凭证错误预警
- 增加卡密自动领取、管理员账号管理、未连接账号清理和微信扫码全局配置
- 优化农作物策略分析、种植和枯地处理；移除高风险秒偷、种子出售和启动时自动连接账号

### 2026-03-12 至 2026-03-14

- 增加背包活动种子优先种植和第二优先策略，修复对应策略选择问题
- 增加批量出售、金豆豆展示、好友分页与缓存，修复推送和冬虫夏草放置
- 修复默认主题显示，并调整水印和提示弹窗行为

### 2026-03-08 至 2026-03-11

- 完善额度卡密、时间卡密、公告管理和用户权限隔离
- 增加背包分类、种子果实出售、道具使用、访客记录、GID 导入清理与好友信息展示
- 支持 2x2 作物占地、购买和种植，增加合种标识；修复出售开关、巡视、封禁黑名单和肥料购买
- 增加施肥土地选择、二季施肥、自定义防偷时间、管理员 Token 展示和界面折叠
- 更新登录协议并修复 QQ 登录、客户端版本和好友巡查倒计时
